name = "wizamp-worker"
main = "src/index.js"
compatibility_date = "2024-09-01"

[durable_objects]
bindings = [
  { name = "ROOM_HUB", class_name = "RoomHub" }
]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomHub"]

# If you plan to bind a custom domain later:
# routes = [
#   { pattern = "api.yourdomain.com/*", custom_domain = true }
# ]
